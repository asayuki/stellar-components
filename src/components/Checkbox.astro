---
const { checked = false, label = '', id = '' } = Astro.props;
---

<div class="checkbox-container">
    <input type="checkbox" id={id} checked={checked} />
    {label && <label for={id}>{label}</label>}
</div>

<style>
    .checkbox-container {
        display: flex;
        align-items: center;
    }

    .checkbox-container input[type="checkbox"]:not(:only-child) {
        margin-right: 8px;
    }
</style></div>